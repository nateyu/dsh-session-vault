/** Shared RPC channel and endpoint names for the Host handler and settings page. */

export const VAULT_RPC_CHANNEL = '/dsh-session-vault'

export const VAULT_ENDPOINTS = Object.freeze({
  list: 'vault.list',
  delete: 'vault.delete',
})
